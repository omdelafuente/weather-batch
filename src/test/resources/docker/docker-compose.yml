version: '3.0'
services:
  weather-db:
    image: postgres:9.6.6-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=weather
      - POSTGRES_USER=weather_batch
      - POSTGRES_PASSWORD=0wc3lbmu